var config = {
    appId: 'com.sloweating.app',
    appName: 'Slow Eating',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    },
    plugins: {
        Haptics: {
        // Haptics plugin configuration
        }
    }
};
export default config;
